Для урока по установке Linux в виртуальную среду и его первоначальной настройке можно построить план следующим образом:

---

### **1. Выбор виртуальной среды**
- Обзор популярных решений:
  - VirtualBox (кроссплатформенная, удобная для обучения)
  - VMware Workstation Player (более продвинутая, но требует лицензию для коммерческого использования)
  - QEMU/KVM (оптимально для Linux)
  - Hyper-V (встроенный в Windows)
- Выбор ISO-образа дистрибутива (например, AlmaLinux, Ubuntu, Debian)

---

### **2. Установка виртуальной машины**
- **Создание новой виртуальной машины**
  - Выбор объема оперативной памяти (рекомендуется 2–4 ГБ)
  - Настройка виртуального жесткого диска (10–20 ГБ)
  - Выбор загрузочного ISO-образа

- **Процесс установки Linux**
  - Разделение диска (автоматически или вручную)
  - Выбор часового пояса и раскладки клавиатуры
  - Создание пользователя и установка пароля root (если применимо)

---

### **3. Первоначальная настройка системы**
- Установка обновлений после первого входа:
  ```bash
  sudo apt update && sudo apt upgrade -y   # Для Ubuntu/Debian
  sudo dnf update -y                      # Для AlmaLinux/CentOS
  ```
- Установка необходимых утилит:
  ```bash
  sudo apt install net-tools vim curl -y   # Ubuntu/Debian
  sudo dnf install net-tools vim curl -y   # AlmaLinux
  ```

---

### **4. Настройка сетевых интерфейсов**
Разбор всех вариантов настройки IP-адресов:

#### **4.1 DHCP (автоматическое получение IP)**
- В Ubuntu (Netplan):
  ```yaml
  network:
    version: 2
    ethernets:
      eth0:
        dhcp4: true
  ```
  Применение:
  ```bash
  sudo netplan apply
  ```

- В AlmaLinux:
  ```bash
  nmcli con mod eth0 ipv4.method auto
  nmcli con up eth0
  ```

#### **4.2 Статический IP**
- В Ubuntu (Netplan):
  ```yaml
  network:
    version: 2
    ethernets:
      eth0:
        dhcp4: false
        addresses:
          - 192.168.1.100/24
        gateway4: 192.168.1.1
        nameservers:
          addresses:
            - 8.8.8.8
  ```
  Применение:
  ```bash
  sudo netplan apply
  ```

- В AlmaLinux (через nmcli):
  ```bash
  nmcli con mod eth0 ipv4.addresses 192.168.1.100/24
  nmcli con mod eth0 ipv4.gateway 192.168.1.1
  nmcli con mod eth0 ipv4.dns "8.8.8.8"
  nmcli con mod eth0 ipv4.method manual
  nmcli con up eth0
  ```

#### **4.3 Настройка через файлы конфигурации**
- В **Ubuntu/Debian** (старый способ — `/etc/network/interfaces`):
  ```ini
  auto eth0
  iface eth0 inet static
      address 192.168.1.100
      netmask 255.255.255.0
      gateway 192.168.1.1
      dns-nameservers 8.8.8.8 8.8.4.4
  ```

- В **AlmaLinux** (через `/etc/sysconfig/network-scripts/ifcfg-eth0`):
  ```ini
  DEVICE=eth0
  BOOTPROTO=static
  ONBOOT=yes
  IPADDR=192.168.1.100
  NETMASK=255.255.255.0
  GATEWAY=192.168.1.1
  DNS1=8.8.8.8
  ```
  Применение:
  ```bash
  systemctl restart network
  ```

---

### **5. Проверка сетевой конфигурации**
Команды для диагностики:
```bash
ip a                 # Просмотр IP-адресов
ip r                 # Просмотр маршрутов
ping 8.8.8.8         # Проверка подключения к интернету
nslookup google.com  # Проверка DNS
```

---


Для одноразовой настройки сети в Linux можно использовать команды **ifconfig**, **route** и **resolv.conf**. Эти изменения будут действовать до перезагрузки системы.

---

## **1. Назначение IP-адреса с помощью `ifconfig`**
Команда `ifconfig` используется для настройки сетевых интерфейсов, но в современных дистрибутивах она заменена на `ip`. Однако, в старых системах ее можно использовать.

### **Пример: Назначение статического IP-адреса**
```bash
sudo ifconfig eth0 192.168.1.100 netmask 255.255.255.0 up
```
🔹 **eth0** – имя сетевого интерфейса (можно узнать командой `ip a` или `ifconfig -a`).  
🔹 **192.168.1.100** – нужный IP-адрес.  
🔹 **netmask 255.255.255.0** – маска сети.  
🔹 **up** – включает интерфейс, если он был выключен.  

### **Сброс интерфейса**
Если нужно сбросить настройки:
```bash
sudo ifconfig eth0 0.0.0.0
```

---

## **2. Настройка шлюза по умолчанию через `route`**
Для установки маршрута по умолчанию используется команда `route`:

```bash
sudo route add default gw 192.168.1.1 eth0
```
🔹 **192.168.1.1** – IP-адрес шлюза (обычно это адрес роутера).  
🔹 **eth0** – интерфейс, через который идет выход в сеть.

### **Удаление маршрута**
Если нужно удалить маршрут по умолчанию:
```bash
sudo route del default gw 192.168.1.1 eth0
```

**Современная альтернатива с `ip route` (рекомендуется):**
```bash
sudo ip route add default via 192.168.1.1 dev eth0
```
---

## **3. Настройка DNS через `/etc/resolv.conf`**
Файл `/etc/resolv.conf` отвечает за настройку DNS-серверов. Можно редактировать его вручную:

```bash
sudo nano /etc/resolv.conf
```
Добавить строки:
```
nameserver 8.8.8.8
nameserver 8.8.4.4
```
🔹 Это Google DNS-серверы. Можно заменить на серверы вашего провайдера.

### **Применение изменений без перезагрузки**
После редактирования файла, можно проверить DNS-запросы:
```bash
nslookup google.com
```
или
```bash
dig google.com
```

---

## **4. Проверка конфигурации**
После выполнения всех команд, можно проверить настройки:

- **Текущие IP-адреса интерфейсов**
  ```bash
  ifconfig
  ```
- **Маршруты в системе**
  ```bash
  route -n
  ```
  или
  ```bash
  ip route show
  ```
- **DNS-серверы**
  ```bash
  cat /etc/resolv.conf
  ```

---

### ❗ **Важно!**
Эти настройки временные и будут сброшены после перезагрузки. Чтобы сделать их постоянными, нужно вносить изменения в файлы конфигурации сети (`/etc/network/interfaces`, `netplan`, `nmcli`, `ifcfg-eth0` и т. д.).

---

### **Вывод**
- `ifconfig` – установка IP-адреса.
- `route` – настройка шлюза.
- `resolv.conf` – настройка DNS.
- Эти настройки действуют до перезагрузки системы.



Для настройки сети с помощью утилит в Linux можно использовать различные инструменты, которые зависят от дистрибутива. Рассмотрим наиболее популярные:

---

## **1. Настройка через `nmtui` (Network Manager Text User Interface)**
Это удобная текстовая утилита, работающая в терминале.

### **Установка (если не установлена)**
```bash
sudo apt install network-manager  # Ubuntu/Debian
sudo dnf install NetworkManager-tui  # AlmaLinux/CentOS
```

### **Запуск утилиты**
```bash
sudo nmtui
```
В интерфейсе можно:
- **Редактировать** соединения (установить статический IP, шлюз, DNS).
- **Активировать/деактивировать** соединения.
- **Выбрать и подключиться** к Wi-Fi.

После внесения изменений необходимо перезапустить соединение:
```bash
sudo systemctl restart NetworkManager
```

---

## **2. Настройка через `nmcli` (Network Manager CLI)**
Утилита `nmcli` позволяет настраивать сеть через командную строку.

### **Показать список соединений**
```bash
nmcli con show
```

### **Настроить статический IP**
```bash
nmcli con mod eth0 ipv4.addresses 192.168.1.100/24
nmcli con mod eth0 ipv4.gateway 192.168.1.1
nmcli con mod eth0 ipv4.dns 8.8.8.8
nmcli con mod eth0 ipv4.method manual
nmcli con up eth0
```
🔹 **eth0** – название интерфейса (можно узнать через `ip a`).  
🔹 **192.168.1.100/24** – статический IP.  
🔹 **192.168.1.1** – шлюз.  
🔹 **8.8.8.8** – DNS.  

### **Настроить DHCP (автоматический IP)**
```bash
nmcli con mod eth0 ipv4.method auto
nmcli con up eth0
```

---

## **3. Настройка через `netplan` (Ubuntu 18.04+, Debian 11+)**
В новых версиях Ubuntu используется `netplan`.

### **Редактируем конфигурационный файл**
```bash
sudo nano /etc/netplan/01-netcfg.yaml
```
Пример конфигурации для **статического IP**:
```yaml
network:
  version: 2
  ethernets:
    eth0:
      addresses:
        - 192.168.1.100/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
```
Сохраняем (`CTRL+X`, `Y`, `Enter`), затем применяем:
```bash
sudo netplan apply
```

Для **автоматического получения IP (DHCP)**:
```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: true
```
Применение:
```bash
sudo netplan apply
```

---

## **4. Настройка через `ifupdown` (Debian, старые Ubuntu)**
Редактируем файл `/etc/network/interfaces`:
```bash
sudo nano /etc/network/interfaces
```
Пример **статической конфигурации**:
```
auto eth0
iface eth0 inet static
    address 192.168.1.100
    netmask 255.255.255.0
    gateway 192.168.1.1
    dns-nameservers 8.8.8.8
```
Применяем изменения:
```bash
sudo systemctl restart networking
```

Для **автоматического IP (DHCP)**:
```
auto eth0
iface eth0 inet dhcp
```
Перезапускаем:
```bash
sudo systemctl restart networking
```

---

## **5. Настройка через `Yast` (SUSE Linux Enterprise, openSUSE)**
В openSUSE можно использовать **YaST**:
```bash
sudo yast2
```
Далее выбрать **Network Settings** и настроить сетевой интерфейс через графический интерфейс.

---

### **Заключение**
- `nmtui` – удобная текстовая утилита.
- `nmcli` – мощный инструмент для скриптов и командной строки.
- `netplan` – современный способ в Ubuntu.
- `ifupdown` – классический способ (Debian, старые Ubuntu).
- `YaST` – удобный инструмент для SUSE Linux.



## **Настройка сети через файл `/etc/network/interfaces`**  
Файл `/etc/network/interfaces` используется для настройки сетевых интерфейсов в **Debian**, **старых версиях Ubuntu (до 18.04)** и некоторых других дистрибутивах.  

---

## **1. Открытие файла для редактирования**  
Файл `/etc/network/interfaces` можно открыть с помощью текстового редактора:  
```bash
sudo nano /etc/network/interfaces
```

---

## **2. Настройка DHCP (автоматическое получение IP)**
Если система должна получать IP-адрес от DHCP-сервера (например, от роутера), используем следующую конфигурацию:
```
auto eth0
iface eth0 inet dhcp
```
🔹 **auto eth0** – интерфейс будет автоматически активирован при загрузке.  
🔹 **iface eth0 inet dhcp** – указывает, что IP-адрес будет получен от DHCP.  

После изменения файла примените настройки:
```bash
sudo systemctl restart networking
```
или  
```bash
sudo ifdown eth0 && sudo ifup eth0
```

---

## **3. Настройка статического IP**
Если необходимо задать статический IP-адрес, добавляем в файл следующую конфигурацию:
```
auto eth0
iface eth0 inet static
    address 192.168.1.100
    netmask 255.255.255.0
    gateway 192.168.1.1
    dns-nameservers 8.8.8.8 8.8.4.4
```
🔹 **address** – статический IP-адрес.  
🔹 **netmask** – маска подсети.  
🔹 **gateway** – шлюз по умолчанию.  
🔹 **dns-nameservers** – DNS-серверы (в данном случае Google DNS).  

Применение изменений:
```bash
sudo systemctl restart networking
```

---

## **4. Настройка нескольких интерфейсов**
Если в системе несколько сетевых интерфейсов (например, eth0 и eth1), можно настроить их по-разному:
```
auto eth0
iface eth0 inet dhcp

auto eth1
iface eth1 inet static
    address 192.168.2.100
    netmask 255.255.255.0
    gateway 192.168.2.1
```
Здесь:
- **eth0** получает IP-адрес через DHCP.
- **eth1** настроен статически.

---

## **5. Отключение интерфейса**
Если нужно временно отключить сетевой интерфейс, можно использовать:
```
iface eth0 inet manual
```
Это позволит вручную управлять интерфейсом через `ifup`/`ifdown`:
```bash
sudo ifdown eth0
sudo ifup eth0
```

---

## **6. Настройка алиасов (виртуальных интерфейсов)**
Иногда нужно задать несколько IP-адресов на одном интерфейсе. Для этого используются алиасы:
```
auto eth0
iface eth0 inet static
    address 192.168.1.100
    netmask 255.255.255.0
    gateway 192.168.1.1

auto eth0:1
iface eth0:1 inet static
    address 192.168.1.101
    netmask 255.255.255.0
```
🔹 **eth0:1** – виртуальный интерфейс с дополнительным IP-адресом.  

---

## **7. Настройка IPv6**
Если нужно настроить IPv6-адресацию, добавляем:
```
iface eth0 inet6 static
    address 2001:db8::100
    netmask 64
    gateway 2001:db8::1
```

---

## **8. Проверка настроек**
После применения изменений проверьте конфигурацию:
```bash
ip a           # Просмотр IP-адресов
ip r           # Проверка маршрутов
cat /etc/resolv.conf  # Проверка DNS
ping 8.8.8.8   # Проверка соединения с интернетом
```

---


## **Настройка сети с использованием Netplan**  
Netplan — это утилита для конфигурирования сети в **Ubuntu (18.04 и новее) и Debian 11+**.  

### **1. Где находится конфигурация Netplan?**  
Файлы настроек Netplan хранятся в **`/etc/netplan/`**. Обычные имена файлов:  
- **`/etc/netplan/50-cloud-init.yaml`** (в облачных окружениях)  
- **`/etc/netplan/01-netcfg.yaml`** (основной конфигурационный файл)  

Чтобы редактировать конфигурацию, используйте команду:  
```bash
sudo nano /etc/netplan/01-netcfg.yaml
```

---

## **2. Настройка сети с DHCP (автоматический IP)**
Если сеть должна получать IP-адрес автоматически через **DHCP**, используйте следующую конфигурацию:
```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: true
```
После внесения изменений **примените их**:
```bash
sudo netplan apply
```
🔹 **eth0** – имя сетевого интерфейса (можно узнать командой `ip a`).  
🔹 **dhcp4: true** – включение DHCP для IPv4.  

Если также требуется DHCP для **IPv6**, добавьте строку:
```yaml
      dhcp6: true
```

---

## **3. Настройка статического IP**
Если требуется **статический IP-адрес**, измените файл Netplan:
```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: false
      addresses:
        - 192.168.1.100/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```
Здесь:  
🔹 **`dhcp4: false`** – отключение DHCP.  
🔹 **`addresses:`** – список IP-адресов с маской (`/24` = `255.255.255.0`).  
🔹 **`gateway4:`** – шлюз.  
🔹 **`nameservers:`** – DNS-серверы (Google DNS).  

Применение настроек:
```bash
sudo netplan apply
```

---

## **4. Настройка нескольких интерфейсов**
Если у вас несколько сетевых интерфейсов (например, **eth0 и eth1**), можно настроить их отдельно:
```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: true
    eth1:
      dhcp4: false
      addresses:
        - 192.168.2.100/24
      gateway4: 192.168.2.1
      nameservers:
        addresses:
          - 1.1.1.1
          - 8.8.8.8
```
🔹 **eth0** получает IP-адрес автоматически.  
🔹 **eth1** имеет статический IP, шлюз и DNS.  

---

## **5. Настройка Wi-Fi (если используется)**
Для подключения к Wi-Fi через Netplan:
```yaml
network:
  version: 2
  wifis:
    wlan0:
      dhcp4: true
      access-points:
        "MyWiFi":
          password: "SuperSecretPassword"
```
🔹 **wlan0** – имя беспроводного интерфейса.  
🔹 **"MyWiFi"** – имя Wi-Fi сети (SSID).  
🔹 **password** – пароль от сети.  

Применение настроек:
```bash
sudo netplan apply
```

---

## **6. Настройка нескольких IP-адресов на одном интерфейсе**
Если требуется несколько IP-адресов на одном интерфейсе (`eth0`):
```yaml
network:
  version: 2
  ethernets:
    eth0:
      addresses:
        - 192.168.1.100/24
        - 192.168.1.101/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
```
🔹 Теперь `eth0` имеет **два** IP-адреса: `192.168.1.100` и `192.168.1.101`.

---

## **7. Проверка и отладка конфигурации**
### **Проверка перед применением**  
Перед окончательным применением можно проверить, нет ли ошибок:
```bash
sudo netplan try
```
Если ошибок нет, то можно применять:
```bash
sudo netplan apply
```

### **Просмотр текущих настроек**  
```bash
ip a         # Проверка IP-адресов
ip r         # Проверка маршрутов
cat /etc/resolv.conf  # Проверка DNS
```

---

## **Заключение**
- **Netplan заменил ifupdown в Ubuntu 18.04+ и Debian 11+.**  
- **Настройка через YAML-файлы делает конфигурацию удобной и понятной.**  
- **Изменения применяются командой `netplan apply`.**  
- **Поддерживает как Ethernet, так и Wi-Fi.**  


## **Настройка Wi-Fi из консоли в Linux**
В Linux можно настроить Wi-Fi без графического интерфейса с помощью нескольких утилит, таких как `nmcli`, `iwctl`, `wpa_supplicant`, и `netplan`. Рассмотрим основные способы.

---

### **1. Определение доступных Wi-Fi интерфейсов**
Сначала убедитесь, что система распознает Wi-Fi-адаптер:
```bash
ip a
```
Или:
```bash
iw dev
```
🔹 Если интерфейс Wi-Fi присутствует, он обычно называется **wlan0** (может быть `wlp2s0`, `wlan1` и т. д.).

Если интерфейса нет, проверьте:
```bash
rfkill list
```
Если Wi-Fi заблокирован, разблокируйте его:
```bash
rfkill unblock wifi
```

---

## **2. Подключение к Wi-Fi с помощью `nmcli` (NetworkManager CLI)**
Это самый простой способ в системах с NetworkManager (Ubuntu, Debian, Fedora, AlmaLinux).

### **Просмотр доступных сетей**
```bash
nmcli dev wifi list
```

### **Подключение к сети**
```bash
nmcli dev wifi connect "Имя_сети" password "Пароль_от_WiFi"
```
Если подключение успешно, интерфейс получит IP-адрес автоматически.

### **Проверка соединения**
```bash
nmcli con show --active
```

### **Отключение Wi-Fi**
```bash
nmcli radio wifi off
```
И включение обратно:
```bash
nmcli radio wifi on
```

---

## **3. Подключение к Wi-Fi через `iwctl` (для систем с iwd)**
Этот метод актуален для Arch Linux и некоторых других дистрибутивов.

### **Запуск интерактивного режима**
```bash
iwctl
```
Внутри iwd-оболочки:
```bash
station wlan0 scan
station wlan0 get-networks
station wlan0 connect "Имя_сети"
```
Если сеть защищена, система запросит пароль.

### **Выход из оболочки iwd**
```bash
exit
```

---

## **4. Подключение через `wpa_supplicant` (универсальный метод)**
Этот метод работает во всех дистрибутивах, но требует больше ручных настроек.

### **Создание конфигурационного файла Wi-Fi**
```bash
wpa_passphrase "Имя_сети" "Пароль_от_WiFi" | sudo tee /etc/wpa_supplicant/wpa_supplicant.conf
```
Проверяем содержимое файла:
```bash
cat /etc/wpa_supplicant/wpa_supplicant.conf
```
Если всё верно, запускаем Wi-Fi:
```bash
sudo wpa_supplicant -B -i wlan0 -c /etc/wpa_supplicant/wpa_supplicant.conf
```
🔹 `-B` – фоновый режим.  
🔹 `-i wlan0` – указание интерфейса.  
🔹 `-c` – путь к конфигурационному файлу.

### **Получение IP через DHCP**
```bash
sudo dhclient wlan0
```
Проверяем подключение:
```bash
ping 8.8.8.8
```

---

## **5. Настройка Wi-Fi через Netplan (Ubuntu, Debian)**
Для систем с Netplan можно настроить Wi-Fi, отредактировав файл `/etc/netplan/01-netcfg.yaml`:
```bash
sudo nano /etc/netplan/01-netcfg.yaml
```
Добавляем настройки:
```yaml
network:
  version: 2
  wifis:
    wlan0:
      dhcp4: true
      access-points:
        "Имя_сети":
          password: "Пароль_от_WiFi"
```
Сохраняем (`CTRL+X`, `Y`, `Enter`) и применяем изменения:
```bash
sudo netplan apply
```
---

## **6. Проверка состояния Wi-Fi**
После подключения проверьте:
```bash
ip a             # Проверка IP-адресов
iw dev           # Проверка интерфейсов Wi-Fi
nmcli con show   # Проверка активных соединений
```

---

## **Вывод**
- **`nmcli`** – самый удобный способ для Ubuntu/Debian/Fedora.
- **`iwctl`** – используется в Arch Linux (если включен iwd).
- **`wpa_supplicant`** – универсальный способ для всех дистрибутивов.
- **`netplan`** – для постоянной настройки в Ubuntu.


## **Настройка имени хоста в Linux**
Имя хоста (**hostname**) – это уникальное имя компьютера в сети. Оно необходимо для идентификации машины, работы сетевых сервисов и взаимодействия с другими устройствами.

### **1. Проверка текущего имени хоста**
Чтобы узнать текущее имя хоста, используйте команду:
```bash
hostname
```
или:
```bash
cat /etc/hostname
```

---

## **2. Временное изменение имени хоста**
Временное изменение работает до перезагрузки системы.

### **Изменение имени хоста в текущей сессии**
```bash
sudo hostnamectl set-hostname new-hostname
```
или:
```bash
sudo hostname new-hostname
```
После этого имя хоста изменится, но вернется к старому после перезагрузки.

---

## **3. Постоянное изменение имени хоста**
### **Редактирование файла `/etc/hostname`**
Этот файл содержит текущее имя хоста системы. Чтобы изменить его:
```bash
sudo nano /etc/hostname
```
Замените старое имя на новое, сохраните (`CTRL+X`, `Y`, `Enter`).

### **Редактирование `/etc/hosts`**
Файл `/etc/hosts` используется для локального разрешения имен. Важно обновить его после изменения имени хоста.

Открываем файл:
```bash
sudo nano /etc/hosts
```
Меняем строку:
```
127.0.0.1   old-hostname
```
на:
```
127.0.0.1   new-hostname
```
Если система использует IPv6, также обновляем строку:
```
::1         new-hostname
```
Сохраняем файл.

### **Применение изменений без перезагрузки**
```bash
sudo systemctl restart systemd-logind
exec bash  # Перезапуск текущей сессии
```

---

## **4. Назначение файлов `/etc/hostname` и `/etc/hosts`**
- **`/etc/hostname`** – содержит текущее имя хоста (меняется при загрузке).
- **`/etc/hosts`** – используется для локального сопоставления имен хостов с IP-адресами (аналог локального DNS).

### **Пример файла `/etc/hosts`**
```
127.0.0.1   localhost
127.0.1.1   my-server
192.168.1.100   another-host
```
🔹 **`127.0.0.1 localhost`** – всегда указывает на локальную машину.  
🔹 **`127.0.1.1 my-server`** – часто используется в Debian/Ubuntu.  
🔹 **`192.168.1.100 another-host`** – можно добавить другие хосты.

---

## **5. Проверка изменений**
После изменения имени хоста проверьте:
```bash
hostname
```
```bash
hostnamectl
```
```bash
cat /etc/hostname
```
```bash
cat /etc/hosts
```

---

## **Вывод**
- `hostnamectl set-hostname` – изменяет имя хоста временно или навсегда.
- `/etc/hostname` – файл с именем хоста, который загружается при старте.
- `/etc/hosts` – используется для локального сопоставления IP-адресов и имен.


# **Настройка OpenSSH сервера в Linux**

OpenSSH – это популярный инструмент для удаленного управления сервером по протоколу SSH. В этом руководстве рассмотрим установку OpenSSH-сервера, генерацию ключей, их назначение, расположение, а также разбор конфигурации `/etc/ssh/sshd_config`.

---

## **1. Установка OpenSSH сервера**
В зависимости от дистрибутива команда установки различается.

### **Для Ubuntu/Debian**
```bash
sudo apt update
sudo apt install openssh-server -y
```

### **Для AlmaLinux/RHEL/CentOS**
```bash
sudo dnf install openssh-server -y
```

### **Для Arch Linux**
```bash
sudo pacman -S openssh
```

---

## **2. Проверка и запуск OpenSSH**
После установки проверяем статус сервиса:
```bash
sudo systemctl status ssh
```
Если сервис не запущен, стартуем его:
```bash
sudo systemctl start ssh
```
Чтобы OpenSSH автоматически запускался при загрузке:
```bash
sudo systemctl enable ssh
```

Для RHEL/AlmaLinux/CentOS:
```bash
sudo systemctl enable --now sshd
```

---

## **3. Генерация SSH-ключей**
SSH-ключи позволяют безопасно подключаться без ввода пароля.

### **Генерация ключей на клиенте**
Команда для генерации пары ключей:
```bash
ssh-keygen -t rsa -b 4096
```
🔹 `-t rsa` – используемый алгоритм (можно `ed25519`, `ecdsa`).  
🔹 `-b 4096` – длина ключа (чем больше, тем надежнее).  

После выполнения команды появятся вопросы:
1. **Указать путь для сохранения ключей** (по умолчанию `~/.ssh/id_rsa`).
2. **Придумать пароль (passphrase)** – опционально.

### **Расположение ключей**
После генерации в `~/.ssh/` появятся файлы:
- **`id_rsa`** – закрытый (приватный) ключ. Никому не передавать!
- **`id_rsa.pub`** – открытый (публичный) ключ, который можно загружать на сервер.

### **Копирование ключа на сервер**
Для автоматической передачи публичного ключа используйте:
```bash
ssh-copy-id user@server_ip
```
или вручную добавьте содержимое `id_rsa.pub` в файл на сервере:
```bash
nano ~/.ssh/authorized_keys
```

---

## **4. Конфигурация OpenSSH сервера (`/etc/ssh/sshd_config`)**
Файл конфигурации OpenSSH сервера – `/etc/ssh/sshd_config`. Рассмотрим важные параметры.

### **Основные настройки**
Открываем файл для редактирования:
```bash
sudo nano /etc/ssh/sshd_config
```

#### **Изменение порта SSH**
По умолчанию SSH использует порт `22`. Его можно сменить, например, на `2222`:
```
Port 2222
```
После изменения не забудьте открыть новый порт в `firewalld` или `ufw`.

#### **Отключение входа под root**
Чтобы повысить безопасность, отключите вход под root:
```
PermitRootLogin no
```

#### **Запрет аутентификации по паролю (только по ключам)**
```
PasswordAuthentication no
```
(Важно! Перед этим убедитесь, что ключи работают, иначе можно заблокировать доступ.)

#### **Ограничение доступа по пользователям**
Можно разрешить вход только определенным пользователям:
```
AllowUsers user1 user2
```

#### **Включение логирования**
Для детального логирования событий:
```
LogLevel VERBOSE
```

После изменения настроек **перезапустите SSH-сервер**:
```bash
sudo systemctl restart ssh
```

---

## **5. Проверка работы SSH**
После настройки попробуйте подключиться к серверу:
```bash
ssh user@server_ip -p 2222
```
(`-p 2222` – если был изменен порт)

### **Проверка работающих подключений**
```bash
who
```
или:
```bash
ss -tulpn | grep ssh
```

---

## **6. Открытие порта SSH в брандмауэре**
Если у вас включен **firewalld** (например, в AlmaLinux), добавьте правило:
```bash
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --reload
```
Если порт изменен:
```bash
sudo firewall-cmd --permanent --add-port=2222/tcp
sudo firewall-cmd --reload
```

Для **UFW** (Ubuntu/Debian):
```bash
sudo ufw allow 2222/tcp
sudo ufw reload
```

---

## **Вывод**
- Установили **OpenSSH сервер**.
- Сгенерировали **SSH-ключи** (`id_rsa`, `id_rsa.pub`).
- Разобрали основные параметры **конфигурации (`sshd_config`)**.
- Обеспечили **безопасность** (смена порта, запрет root-входа, отключение пароля).
- Открыли **порт SSH** в брандмауэре.



# **Настройка авторизации по ключам в SSH**
Авторизация по SSH-ключам – это более безопасный способ входа на сервер без использования пароля. В этом руководстве рассмотрим:

✅ Создание ключей с помощью `ssh-keygen`  
✅ Размещение открытого ключа на сервере  
✅ Рекомендации по работе с закрытым ключом  
✅ Оптимизация работы с `ssh-agent`  
✅ Дополнительные возможности  

---

## **1. Генерация SSH-ключей (`ssh-keygen`)**
На клиенте (локальном компьютере) выполните команду:
```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```
🔹 `-t rsa` – использование алгоритма RSA (можно `ed25519`, `ecdsa`).  
🔹 `-b 4096` – длина ключа (чем больше, тем надежнее).  
🔹 `-C "your_email@example.com"` – комментарий (необязательно).  

После выполнения команда спросит:
1. **Путь для сохранения ключей** – по умолчанию `~/.ssh/id_rsa`.  
2. **Пароль (passphrase)** – можно оставить пустым, но для безопасности лучше задать.  

---

## **2. Размещение открытого ключа на сервере**
Открытый ключ (`id_rsa.pub`) нужно добавить на сервер.

### **Автоматическое копирование ключа (`ssh-copy-id`)**
```bash
ssh-copy-id user@server_ip
```
🔹 **user** – имя пользователя на сервере.  
🔹 **server_ip** – IP-адрес или доменное имя сервера.  

### **Ручное копирование**
Если `ssh-copy-id` недоступен, скопируйте содержимое файла `~/.ssh/id_rsa.pub` вручную:
```bash
cat ~/.ssh/id_rsa.pub
```
Добавьте этот ключ в файл **на сервере**:
```bash
nano ~/.ssh/authorized_keys
```
Вставьте содержимое ключа и сохраните (`CTRL+X`, `Y`, `Enter`).

Проверьте права на файлы:
```bash
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

---

## **3. Рекомендации по работе с закрытым ключом**
🔒 **Закрытый ключ (`id_rsa`) нельзя никому передавать!**  

✅ **Используйте сильный passphrase** – это защита в случае кражи ключа.  
✅ **Не загружайте ключ в публичные репозитории (GitHub, GitLab и т. д.).**  
✅ **Используйте ssh-agent** для удобства работы.  

Если нужно удалить старый ключ:
```bash
rm ~/.ssh/id_rsa ~/.ssh/id_rsa.pub
```
Если ключ попал в чужие руки, **немедленно замените его** и удалите старый с сервера.

---

## **4. Оптимизация работы с `ssh-agent`**
`ssh-agent` позволяет хранить закрытый ключ в памяти и не вводить пароль при каждом подключении.

### **Запуск `ssh-agent`**
```bash
eval "$(ssh-agent -s)"
```

### **Добавление ключа**
```bash
ssh-add ~/.ssh/id_rsa
```
Теперь при подключении к серверу пароль вводить не нужно.

### **Проверка загруженных ключей**
```bash
ssh-add -l
```

### **Удаление ключа из агента**
```bash
ssh-add -d ~/.ssh/id_rsa
```
или очистка всех ключей:
```bash
ssh-add -D
```

---

## **5. Отключение паролей на сервере (повышение безопасности)**
Когда авторизация по ключам настроена, можно отключить вход по паролю.

Открываем конфигурацию SSH-сервера:
```bash
sudo nano /etc/ssh/sshd_config
```
Изменяем:
```
PasswordAuthentication no
```
Перезапускаем SSH:
```bash
sudo systemctl restart ssh
```

Теперь вход возможен **только по ключу**. Если ключ потерян, доступ к серверу будет заблокирован!

---

## **6. Дополнительные возможности**
### **Настройка нескольких ключей**
Если нужно использовать несколько ключей, укажите их в `~/.ssh/config`:
```bash
Host myserver
    HostName 192.168.1.100
    User user
    IdentityFile ~/.ssh/id_rsa
```
Теперь можно подключаться просто командой:
```bash
ssh myserver
```

### **Использование ключей с `ssh-agent` на удаленных серверах**
Чтобы пробросить `ssh-agent` на сервер:
```bash
ssh -A user@server_ip
```
Теперь можно заходить с сервера на другой сервер без ввода пароля.

---

## **Вывод**
- **Генерация ключей** – `ssh-keygen -t rsa -b 4096`.  
- **Добавление ключа на сервер** – `ssh-copy-id user@server_ip`.  
- **Использование `ssh-agent`** – для хранения ключей в памяти.  
- **Отключение входа по паролю** – настройка в `/etc/ssh/sshd_config`.  
- **Оптимизация с `~/.ssh/config`** – удобный доступ по алиасам.  




## **Ограничение доступа к SSH по IP для конкретных ключей**
В SSH можно ограничить использование конкретных ключей, разрешая их применять **только с определенных IP-адресов или хостов**. Это достигается настройкой файла **`~/.ssh/authorized_keys`**.

---

### **1. Добавление ограничения по хосту в `authorized_keys`**
В файле **`~/.ssh/authorized_keys`** (на сервере) перед открытым ключом можно добавить параметр `from=`, указывающий разрешенные IP-адреса или домены.

Пример:
```
from="192.168.1.100" ssh-rsa AAAAB3... user@client
```
Здесь:
- **192.168.1.100** – IP-адрес клиента, с которого разрешено подключение.
- **ssh-rsa AAAAB3...** – сам публичный ключ.

Теперь этот ключ можно использовать **только с указанного IP**.

---

### **2. Разрешение нескольких IP-адресов**
Если разрешено подключение с нескольких IP:
```
from="192.168.1.100,192.168.1.101" ssh-rsa AAAAB3... user@client
```
🔹 Можно указать **несколько IP**, разделяя их запятыми.

---

### **3. Разрешение подключения по доменному имени**
Если клиент использует **динамический IP**, можно указать **доменное имя**:
```
from="trustedhost.example.com" ssh-rsa AAAAB3... user@client
```
SSH автоматически проверит соответствие доменного имени и IP.

---

### **4. Использование маски подсети**
Чтобы разрешить подключение с целой подсети:
```
from="192.168.1.*" ssh-rsa AAAAB3... user@client
```
или:
```
from="192.168.1.0/24" ssh-rsa AAAAB3... user@client
```
🔹 Это разрешает доступ с **любого IP в сети 192.168.1.0/24**.

---

### **5. Комбинирование ограничений**
Можно добавить **несколько параметров**:
```
from="192.168.1.100" command="/usr/bin/echo Access Denied" ssh-rsa AAAAB3... user@client
```
Здесь:
- **Если подключение идет с другого IP**, сервер выполнит команду `echo Access Denied` вместо запуска сессии.

---

### **6. Проверка логов отказов в доступе**
Если клиент не может подключиться из-за ограничений, посмотрите логи SSH:
```bash
sudo journalctl -u ssh -n 50
```
или:
```bash
sudo tail -f /var/log/auth.log
```

---

## **Вывод**
- В файле **`~/.ssh/authorized_keys`** можно **ограничить доступ по IP или домену**.
- Параметр **`from="IP,IP2,hostname"`** контролирует, откуда можно подключаться.
- **Поддерживаются маски подсетей (`192.168.1.0/24`).**
- **Можно комбинировать с другими ограничениями (`command=...`).**




## **Ограничение команд для SSH-ключей в `authorized_keys`**
В SSH можно **ограничить команды**, которые разрешено выполнять при подключении по **определенному ключу**. Это делается в файле **`~/.ssh/authorized_keys`**, где перед публичным ключом указываются специальные параметры.

---

## **1. Ограничение выполнения только одной команды**
Можно запретить запуск любых команд, кроме одной определенной.

Пример: разрешить только команду `ls /home/user`:
```bash
command="ls /home/user" ssh-rsa AAAAB3... user@client
```
🔹 При попытке выполнить **любую другую команду** сеанс SSH закроется.

---

## **2. Запрет доступа к интерактивному shell**
Если нужно разрешить только выполнение команд, но **запретить интерактивный доступ**, можно сделать так:
```bash
command="echo 'Interactive shell is disabled'; exit" ssh-rsa AAAAB3... user@client
```
Теперь при входе SSH сразу завершится с сообщением.

---

## **3. Ограничение доступа к конкретному скрипту**
Можно разрешить выполнение только одного скрипта:
```bash
command="/home/user/backup.sh" ssh-rsa AAAAB3... user@client
```
🔹 Теперь этот ключ можно использовать **только для запуска `backup.sh`**, другие команды недоступны.

---

## **4. Разрешение нескольких команд**
Если нужно разрешить несколько команд, можно использовать `sh -c`:
```bash
command="sh -c 'ls /home/user && whoami'" ssh-rsa AAAAB3... user@client
```
🔹 Здесь разрешены **`ls /home/user` и `whoami`**, но **ничего больше**.

---

## **5. Ограничение команд + контроль IP-адреса**
Можно комбинировать **ограничение команд** и **ограничение IP-адреса**:
```bash
from="192.168.1.100" command="/home/user/backup.sh" ssh-rsa AAAAB3... user@client
```
🔹 **Этот ключ можно использовать только с IP `192.168.1.100` и только для запуска `backup.sh`**.

---

## **6. Отключение перенаправления, X11 и других возможностей**
Чтобы повысить безопасность, можно отключить:
- **Перенаправление портов** (`no-port-forwarding`)
- **Перенаправление X11** (`no-X11-forwarding`)
- **Использование `pty` (псевдотерминала)** (`no-pty`)

Пример:
```bash
no-pty,no-port-forwarding,no-X11-forwarding,command="/home/user/backup.sh" ssh-rsa AAAAB3... user@client
```
Теперь:
✅ **Запустится только `backup.sh`**  
❌ **Нельзя открыть терминал (`no-pty`)**  
❌ **Нельзя перенаправлять порты**  
❌ **Нельзя использовать X11**  

---

## **7. Проверка ограничений**
Чтобы проверить, какие команды выполняются при авторизации:
```bash
ssh user@server "echo 'Test SSH command execution'"
```
Если команда запрещена – сеанс завершится.

---

## **Вывод**
- **Можно ограничить ключ SSH только одной командой (`command="ls /home/user"`).**  
- **Можно запретить интерактивный доступ (`no-pty`).**  
- **Можно разрешить только запуск определенных скриптов (`command="/home/user/backup.sh"`).**  
- **Можно комбинировать с `from="IP"` для большей безопасности.**  


# **Дополнительные ограничения при авторизации по SSH-ключам**
Помимо ограничения команд и IP-адресов в `authorized_keys`, можно настроить **автообновление системы (`unattended-upgrades`)** и **ротацию логов (`logrotate`)** для повышения безопасности и автоматизации.

---

## **1. Автоматическое обновление системы (`unattended-upgrades`)**
### **📌 Зачем нужно?**
Позволяет автоматически устанавливать критические обновления безопасности без вмешательства пользователя.

---

### **🔹 Установка `unattended-upgrades`**
#### **Ubuntu / Debian**
```bash
sudo apt update && sudo apt install unattended-upgrades -y
```

#### **AlmaLinux / RHEL / CentOS (Dnf-automatic)**
```bash
sudo dnf install dnf-automatic -y
```

---

### **🔹 Включение автоматического обновления**
#### **Ubuntu / Debian**
Включаем службу:
```bash
sudo dpkg-reconfigure unattended-upgrades
```
Редактируем файл `/etc/apt/apt.conf.d/50unattended-upgrades`:
```bash
sudo nano /etc/apt/apt.conf.d/50unattended-upgrades
```
Добавляем строки:
```plaintext
Unattended-Upgrade::Allowed-Origins {
    "origin=Debian,codename=${distro_codename}-security";
    "origin=Ubuntu,codename=${distro_codename}-security";
};
Unattended-Upgrade::Automatic-Reboot "true";  # Автоперезагрузка при необходимости
```

#### **AlmaLinux / RHEL / CentOS**
Редактируем конфигурацию:
```bash
sudo nano /etc/dnf/automatic.conf
```
Находим и меняем:
```ini
apply_updates = yes
```
Включаем службу:
```bash
sudo systemctl enable --now dnf-automatic.timer
```

---

### **🔹 Проверка работы обновлений**
```bash
journalctl -u unattended-upgrades -n 50
```
или
```bash
sudo systemctl status unattended-upgrades
```
В CentOS:
```bash
sudo journalctl -u dnf-automatic.timer
```

---

## **2. Настройка ротации логов (`logrotate`)**
### **📌 Зачем нужно?**
Ротация логов позволяет **автоматически очищать старые логи**, уменьшая потребление дискового пространства.

---

### **🔹 Проверка установки `logrotate`**
#### **Ubuntu/Debian**
```bash
sudo apt install logrotate -y
```
#### **AlmaLinux / CentOS / RHEL**
```bash
sudo dnf install logrotate -y
```

---

### **🔹 Основной конфигурационный файл**
Файл: `/etc/logrotate.conf`
Пример:
```ini
# Сохранение логов 30 дней
rotate 30

# Сжатие старых логов
compress

# Не создавать пустые файлы после очистки
nocreate
```

Дополнительные настройки для отдельных сервисов находятся в `/etc/logrotate.d/`.

---

### **🔹 Пример ротации логов SSH**
Создаем конфигурацию `/etc/logrotate.d/sshd`:
```bash
sudo nano /etc/logrotate.d/sshd
```
Добавляем:
```ini
/var/log/auth.log {
    weekly         # Ротация раз в неделю
    rotate 5       # Хранить 5 копий
    compress       # Сжимать архивы
    missingok      # Игнорировать, если файла нет
    notifempty     # Не обрабатывать пустые файлы
    postrotate
        systemctl restart sshd
    endscript
}
```
🔹 Это будет **сжимать и хранить 5 последних недель логов SSH**.

---

### **🔹 Проверка работы `logrotate`**
```bash
sudo logrotate -d /etc/logrotate.conf
```
или принудительный запуск:
```bash
sudo logrotate -f /etc/logrotate.conf
```

---

## **Вывод**
- **`unattended-upgrades`** автоматически обновляет систему.
- **`logrotate`** управляет логами и экономит место.
- **Эти настройки повышают безопасность и снижают нагрузку на администратора.**



# **Практическое занятие: Создание копии сервера и настройка сети в CentOS (AlmaLinux/RHEL)**

## **Цель практики**  
✅ Создать резервную копию сервера.  
✅ Развернуть сервер из копии.  
✅ Настроить сеть в дистрибутивах на базе Red Hat (CentOS, AlmaLinux, RHEL).  

---

## **1. Создание резервной копии сервера**
Рассмотрим несколько способов резервного копирования.

### **🔹 1.1. Копирование с помощью `tar` (архив всей системы)**
Создаем полный архив системы (исключая динамические каталоги `/proc`, `/sys`, `/dev`, `/run` и т. д.):
```bash
sudo tar --exclude=/proc --exclude=/sys --exclude=/dev --exclude=/run --exclude=/mnt --exclude=/tmp --exclude=/var/tmp -cvpzf /mnt/backup-server.tar.gz /
```
🔹 **`-c`** – создать архив.  
🔹 **`-v`** – показывать процесс.  
🔹 **`-p`** – сохранять права доступа.  
🔹 **`-z`** – сжать архив.  
🔹 **`-f`** – указать имя файла.  

💾 **Рекомендация:** Сохраняйте архив на внешний диск (`/mnt/backup/`) или в облако.

### **🔹 1.2. Создание образа диска с `dd`**
Если необходимо полностью клонировать диск, используем:
```bash
sudo dd if=/dev/sda of=/mnt/server-backup.img bs=4M status=progress
```
🔹 `if=/dev/sda` – исходный диск.  
🔹 `of=/mnt/server-backup.img` – куда сохранить образ.  
🔹 `bs=4M` – размер блока.  

---

## **2. Восстановление сервера из резервной копии**
### **🔹 2.1. Восстановление из архива `tar`**
На новом сервере загружаемся с LiveCD и выполняем:
```bash
sudo tar xvpfz /mnt/backup-server.tar.gz -C /
```
Далее пересоздаем загрузочный раздел:
```bash
sudo grub2-install /dev/sda
sudo grub2-mkconfig -o /boot/grub2/grub.cfg
```
Перезагружаем систему.

### **🔹 2.2. Восстановление из образа `dd`**
```bash
sudo dd if=/mnt/server-backup.img of=/dev/sda bs=4M status=progress
```
После восстановления **перепроверьте конфигурацию сети!**

---

## **3. Настройка сети в Red Hat (CentOS, AlmaLinux, RHEL)**
После восстановления сервера может потребоваться настройка сети.

### **🔹 3.1. Определение сетевого интерфейса**
Проверяем доступные интерфейсы:
```bash
ip a
```
или
```bash
nmcli device status
```
Допустим, интерфейс называется `ens192`.

---

### **🔹 3.2. Настройка сети через `nmcli`**
В CentOS 8+ используется **NetworkManager**.

#### **Статический IP**
```bash
nmcli con add type ethernet ifname ens192 con-name static-ens192 ipv4.addresses 192.168.1.100/24 ipv4.gateway 192.168.1.1 ipv4.dns 8.8.8.8 ipv4.method manual
```
Применяем настройки:
```bash
nmcli con up static-ens192
```

#### **DHCP (автоматический IP)**
```bash
nmcli con mod ens192 ipv4.method auto
nmcli con up ens192
```

---

### **🔹 3.3. Настройка через файлы (`ifcfg-ethX`)**
Редактируем конфигурационный файл `/etc/sysconfig/network-scripts/ifcfg-ens192`:
```bash
sudo nano /etc/sysconfig/network-scripts/ifcfg-ens192
```
Для **статического IP**:
```ini
DEVICE=ens192
BOOTPROTO=static
ONBOOT=yes
IPADDR=192.168.1.100
NETMASK=255.255.255.0
GATEWAY=192.168.1.1
DNS1=8.8.8.8
```
Сохраняем и перезапускаем сеть:
```bash
sudo systemctl restart NetworkManager
```

---

### **🔹 3.4. Проверка настроек сети**
```bash
ip a
ip r
ping 8.8.8.8
```

---

## **Вывод**
- 📌 **Создали резервную копию сервера (`tar`, `dd`).**  
- 📌 **Восстановили сервер из резервной копии.**  
- 📌 **Настроили сеть в CentOS (`nmcli`, `ifcfg-*`).**  



